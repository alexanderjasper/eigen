id: chg-001
sequence: 1
timestamp: "2026-03-20T21:00:00Z"
author: alexander
type: created
summary: Initial spec for eigen spec show
reason: |
  Extracted from the monolithic spec-cli spec.
status: compiled
changes:
  title: spec show
  owner: eigen
  status: draft
  description: |
    The spec show command prints the current spec.yaml of a module to stdout.
  behavior: |
    Accepts a single slash-delimited path argument. Reads and prints
    specs/<path>/spec.yaml verbatim to stdout.
  acceptance_criteria:
    - id: AC-001
      description: Prints the current spec.yaml to stdout
      given: A valid existing module
      when: eigen spec show <path> is run
      then: Prints the contents of spec.yaml to stdout
